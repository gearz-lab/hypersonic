var ComponentResolver = require('../src/lib/ComponentResolver');

describe('Process environment for tests', function () {
    it('Should be development for React console warnings', function () {
        assert.ok(ComponentResolver);
    });
});