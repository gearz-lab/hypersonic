var React = require('react');
var Router = require('react-router');

var About = React.createClass({

    render: function() {
        return (
            <div>
                Geaz is a platform for creating data-centric business apps
            </div>
        );
    }
});

export default About;